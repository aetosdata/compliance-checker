{
  "name": "compliance-checker-proxy",
  "version": "1.0.0",
  "description": "Proxy function for the compliance checker.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^2.6.1"
  }
}
